## 1. Scene lifecycle

- [ ] 1.1 Implement `BootScene` (config init → Preload)
- [ ] 1.2 Implement `PreloadScene` with progress bar and asset loading via `assetKeys`
- [ ] 1.3 Implement `MainMenuScene` with start action → GameScene
- [ ] 1.4 Implement `GameScene` thin update loop calling movement use case
- [ ] 1.5 Implement `GameOverScene` with restart → GameScene and menu → MainMenuScene
- [ ] 1.6 Register all scenes in central bootstrap with stable scene keys
