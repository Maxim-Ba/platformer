## 1. Scene lifecycle

- [x] 1.1 Implement `BootScene` (config init → Preload)
- [x] 1.2 Implement `PreloadScene` with progress bar and asset loading via `assetKeys`
- [x] 1.3 Implement `MainMenuScene` with start action → GameScene
- [x] 1.4 Implement `GameScene` thin update loop calling movement use case
- [x] 1.5 Implement `GameOverScene` with restart → GameScene and menu → MainMenuScene
- [x] 1.6 Register all scenes in central bootstrap with stable scene keys
